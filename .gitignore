# Dependencies — never commit
node_modules/

# Runtime secrets — never commit
.env

# YouTube cookies (sensitive)
data/cookies.txt

# Runtime data — volatile, personal, not for repo
data/activity-log.json
data/downloads.json
data/settings.json

# PIDs and logs
data/server.pid
data/tunnel.pid
data/server.log
data/tunnel.log
data/healthcheck.log

# .claude openspec runtime files (internal dev tooling)
.claude/

# data/categories.json is intentionally kept (non-sensitive, useful template)
